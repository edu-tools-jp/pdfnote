# PDFノートを GitHub Pages で公開して「更新ボタン」で使う手順

このアプリを **GitHub Pages** で公開すると、次のようになります。

- 先生は **URL をブックマークして開くだけ**（インストール不要）。
- **初回に一度開けば、以降はオフラインでも起動**します（授業中にネットが不安定でも使えます）。
- あなたがアプリを更新して GitHub に上げ直すと、先生は画面の **「🔄 更新」ボタン** を押すだけで最新版になります（再インストール不要）。
- 書き込みデータは今まで通り、先生が選んだ **フォルダ（Googleドライブ等）** に保存されます。公開しても中身（生徒のデータ）は世界には出ません。アプリの「プログラム」だけが公開されます。

> ⚠️ GitHub Pages は「公開ページ」です。**アプリのコードは誰でも見られます**（教材の中身ではなく、プログラムだけ）。生徒の書き込みや取り込んだPDFは各PCのフォルダに保存され、GitHubには一切上がりません。

---

## 手順1：`edutools` リポジトリを作る

1. GitHub にログイン →右上「＋」→ **New repository**。
2. **Repository name** に `edutools` と入力。
3. **Public**（公開）を選ぶ。※GitHub Pages を無料で使うには Public が簡単です。
4. **Create repository**。

## 手順2：ファイルを上げる（コマンド不要・ドラッグ＆ドロップ）

配布された ZIP（`edutools-pages.zip`）を解凍すると、中に次の形が入っています。

```
edutools/
├── index.html          ← ツール一覧（入口ページ）
└── pdf-note/           ← PDFノート アプリ一式
    ├── index.html
    ├── service-worker.js
    ├── manifest.webmanifest
    ├── icon.svg
    ├── js/ …
    └── vendor/ …
```

1. さきほど作った `edutools` リポジトリのページで **「uploading an existing file」** をクリック
   （または「Add file」→「Upload files」）。
2. 解凍した **`edutools` フォルダの中身**（`index.html` と `pdf-note` フォルダ）を、
   ブラウザの点線枠に **まとめてドラッグ＆ドロップ**します。
   - フォルダごとドラッグできない場合は、`pdf-note` の中身を保ったまま
     何回かに分けてアップロードしてください（フォルダ構造は維持されます）。
3. 下の **Commit changes**（緑ボタン）を押して確定。

## 手順3：GitHub Pages を有効にする

1. `edutools` リポジトリの **Settings** → 左メニュー **Pages**。
2. **Build and deployment** の **Source** を **「Deploy from a branch」**。
3. **Branch** を **`main`**、フォルダを **`/ (root)`** にして **Save**。
4. 1〜2分待つと、ページ上部に公開URLが出ます：
   ```
   https://（あなたのGitHubユーザー名）.github.io/edutools/
   ```

## 手順4：先生に配る

- **入口**： `https://（ユーザー名）.github.io/edutools/`
- **PDFノート直リンク**： `https://（ユーザー名）.github.io/edutools/pdf-note/`

このURLを各PCの Chrome / Edge でブックマーク（またはデスクトップにショートカット）してもらいます。
初回に開くと、以降は**オフラインでも起動**できます。

> Chrome/Edge のアドレスバー右の「インストール」アイコンから
> **デスクトップアプリのように**インストールすることもできます（任意）。

---

## アプリを更新するとき（あなたの作業）

1. アプリのファイルを新しいものに差し替えて、`edutools` リポジトリに上げ直します
   （GitHub の web 画面で該当ファイルを開き「編集」または「Upload files」で上書き）。
2. **重要**：版番号を **2か所**、同じ値に書き換えてください。片方だけだと
   「表示中のファイルが古いようです」という案内が出ます。

   ```js
   // service-worker.js
   const VERSION = '20260824a';   // ← これを新しい値に変える

   // js/app.js
   const APP_VERSION = '20260824a';   // ← 上とまったく同じ値にする
   ```

   書式は「更新日＋枝番」＝ `20260824a`（同じ日に2回目なら `20260824b`）。
   `service-worker.js` の VERSION が変わらないと、ブラウザは「更新なし」と判断して新版を取りに行きません。

3. 先生側は、アプリの **「🔄 更新」ボタン** を押す（または一度閉じて開き直す）と最新版になります。
   - 新版があるときは、ボタンが緑色に光って **「🔄 更新（新版あり）」** になります。

> まとめ：**更新のたびに `service-worker.js` の VERSION を書き換える** ——これだけ忘れなければ、
> 先生は「更新」ボタンだけで新機能を受け取れます。

---

## よくある質問

**Q. オフラインで本当に使える？**
A. 初回にオンラインで一度開けば、アプリ本体はPCにキャッシュされ、次からはネットが無くても起動します。
書き込みの保存先が Googleドライブの場合、オフライン中の保存はPC内にたまり、ネット復帰時に同期されます
（「オフラインでも使えるようにする」設定をドライブ側でしておくと安心）。

**Q. データは消えない？**
A. データは先生が選んだフォルダ（Googleドライブ等）にあり、公開・更新とは無関係です。更新してもデータは残ります。
